export default class ObjectManager {
  constructor({
    emitter,
    store
  }) {
    this.emitter = emitter;
    this.store = store;
    this.objects = {};
  }


  addObject = (objectType) => {
    
  }


  init = () => {


  }

}